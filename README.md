# Face-Tagger

The Fly.io deployment now writes edited CSVs into persistent volume storage at `/data/face-tagger`, so completed work survives deploys and restarts. Completed movies can be exported from the UI, and reopening a completed movie requires the universal password.
